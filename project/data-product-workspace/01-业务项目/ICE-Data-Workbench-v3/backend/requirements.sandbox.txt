# Python 沙箱 (backend/.venv-sandbox/) 白名单依赖
# data-analysis agent 的 execute_python 工具运行时仅有这些包可用
# 不允许：requests / urllib3 / httpx / aiohttp / paramiko / pymysql / 任何网络库
#         （沙箱里 socket 已被 monkey-patch raise，但规避错误依赖也是好的卫生）

# 数据基础
numpy>=1.26
pandas>=2.2
pyarrow>=15

# 统计 / 机器学习
scipy>=1.13
scikit-learn>=1.4
statsmodels>=0.14

# 时序预测
prophet>=1.1

# 变点检测
ruptures>=1.1

# 可视化
matplotlib>=3.8
seaborn>=0.13
