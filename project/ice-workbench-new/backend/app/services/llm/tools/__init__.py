"""Tool implementations — split from tool_runner.py for maintainability."""
# Lazy imports: each tool function is imported on first use by _DISPATCH
# in tool_runner.py. No eager imports here to avoid circular deps.
