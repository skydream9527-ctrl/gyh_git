# Secrets — never commit real keys to git.
# Note: `make pack` DOES include .env in the zip for portable deploys.
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
backend/.venv/
backend/.venv-sandbox/
backend/build/
backend/dist/

# Node
node_modules/
frontend/dist/

# Runtime data (G3 file-first storage; populated at runtime)
.cache/
**/.cache/
users/*
!users/README.md
tasks/*
!tasks/README.md
agents/**/prompt/cards.md
*.lock
*.lock~
.tmp-*

# Logs
*.log
/tmp/uvicorn*.log
/tmp/uv-*.log

# Claude Code local settings (per-user, includes runtime PIDs/IPs)
.claude/settings.local.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# Large extracted reference (legacy v2 codebase, not part of v3 build)
extracted/
ice_data_workspace_v2.zip

# Distributable bundles produced by `make pack`
ice-workbench-*.zip

# 工作 staging — 不入库
# update_file/ : skill 上传 zip 暂存
# test_file/   : agent 测试样本
# SQL 口径...  : 工作底稿（需要保留时单独 git add -f）
# rewrite_guide.py : 一次性 design 改写脚本
update_file/
test_file/
SQL口径与取数表清单.md
rewrite_guide.py
