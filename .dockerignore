# Build context 排除清单 —— 与 Makefile pack target 保持一致
# 目的：让 `docker build` 上下文小、build 快、不把 venv/runtime data 灌进镜像

# Python venv
backend/.venv
backend/.venv-sandbox

# Frontend
frontend/node_modules
frontend/dist

# Runtime data（容器里靠 bind mount 提供，不入镜像）
.cache
users/[a-f0-9]*
tasks/[a-f0-9]*
files/*
!files/.gitkeep
!files/README*

# Cache / temp
**/__pycache__
**/*.pyc
**/*.pyo
**/.pytest_cache
**/.ruff_cache
**/.mypy_cache

# Git / IDE
.git
.gitignore
.vscode
.idea
.DS_Store

# 历史 design 资源（保留 v5）
design_handoff
design_v3
design_v4

# 工作底稿 / 临时
extracted
test_file
update_file
*.log
*.lock
*.zip

# 文档（除了 README + docs/ 目录里的）
ice_data_workspace_v2.zip
SQL口径与取数表清单.md

# Docker 自身（避免把 Dockerfile 改动当作 build context 变化）
.dockerignore
Dockerfile
docker-compose.yml
docker-compose.*.yml
